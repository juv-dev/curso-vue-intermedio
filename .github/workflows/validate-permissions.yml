name: Validate Permissions

on:
  pull_request:
    branches: [main, develop]

jobs:
  validate-branch:
    uses: devops/workflows/.github/workflows/validate-branch.yml@main
    with:
      allowed_branches: "main,develop"
