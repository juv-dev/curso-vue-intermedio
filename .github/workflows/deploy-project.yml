name: Deploy Project

on:
  push:
    branches: [main]
    paths:
      - 'package.json'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Get version
        id: version
        run: echo "version=$(node -p 'require("./package.json").version')" >> $GITHUB_OUTPUT
      
      - name: Deploy
        run: |
          echo "🚀 Desplegando versión ${{ steps.version.outputs.version }}"
          # Aquí van los pasos de despliegue reales